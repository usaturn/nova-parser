"""プロンプト定数の集約モジュール。"""

DOCAI_EXTRACT_PROMPT = """\
以下はTRPGルールブックのページから OCR で抽出したテキストです。
このテキストに含まれるゲームデータを抽出してください。

以下はデータ型の例です:
- スキル: 名称, ルビ, 技能, 上限, タイミング, 対象, 射程, 目標値, 対決, 解説
- 防具: 名称, ルビ, 購, 隠, 防S, 防P, 防I, 制, 電制, 部位, 解説
- サービス: 名称, ルビ, 購, 隠, 電制, 部位, 解説
- ニューラルウェア: 名称, ルビ, 購, 隠, 電制, 部位, 解説

上記以外のデータ型がテキストにある場合は、適切な型名とフィールドを定義して抽出してください。
該当するデータがない場合はtypesを空配列にしてください。
各項目の値は原文をできるだけ忠実に抽出してください。

出力は以下のJSON形式に従ってください:
{"types": [{"type_name": "白兵武器", "items": [{"名称": "...", "ルビ": "...", ...}]}]}

--- OCR テキスト ---
"""

SCHEMA_EXTRACT_PROMPT = """\
以下はTRPGルールブックのページから OCR で抽出したテキストです。
指定されたスキーマに従ってゲームデータを抽出してください。

## スキーマ定義
{schema_section}

## 抽出ルール
1. スキーマのいずれかの型に該当するデータは matched_types に格納してください。
   フィールドは必ずスキーマ定義の名称に従ってください。
2. どのスキーマにも該当しないデータは unmatched_types に格納し、
   適切な type_name と fields を定義してください。
3. 各項目の値は原文をできるだけ忠実に抽出してください。
4. 該当データがない場合は各配列を空にしてください。

## 出力JSON形式
{{"matched_types": [{{"type_name": "武器", "items": [{{"名称": "...", ...}}]}}], \
"unmatched_types": [{{"type_name": "未知の型", "items": [{{"フィールド": "..."}}]}}]}}

--- OCR テキスト ---
{ocr_text}"""

GAMEDATA_PROMPT = """\
この画像はTRPGルールブックのページです。
画像に含まれるゲームデータを抽出してください。

以下はデータ型の例です:
- スキル: 名称, ルビ, 技能, 上限, タイミング, 対象, 射程, 目標値, 対決, 解説
- 防具: 名称, ルビ, 購, 隠, 防S, 防P, 防I, 制, 電制, 部位, 解説
- サービス: 名称, ルビ, 購, 隠, 電制, 部位, 解説

上記以外のデータ型が画像にある場合は、適切な型名とフィールドを定義して抽出してください。
該当するデータがない場合はtypesを空配列にしてください。

出力は以下のJSON形式に従ってください:
{"types": [{"type_name": "白兵武器", "items": [{"名称": "...", "ルビ": "...", ...}]}]}
"""

SCHEMA_DISCOVER_PROMPT = """\
この画像はTRPGルールブックのページです。
画像に含まれるゲームデータの型名とフィールド名を特定してください。
データの中身は不要です。型の定義（カラム名）のみ出力してください。

以下はデータ型の例です:
- スキル: 名称, ルビ, 技能, 上限, タイミング, 対象, 射程, 目標値, 対決, 解説
- 防具: 名称, ルビ, 購, 隠, 防S, 防P, 防I, 制, 電制, 部位, 解説
- サービス: 名称, ルビ, 購, 隠, 電制, 部位, 解説

上記以外のデータ型が画像にある場合は、適切な型名とフィールドを定義してください。
該当するデータがない場合はtypesを空配列にしてください。

出力は以下のJSON形式に従ってください:
{"types": [{"type_name": "白兵武器", "fields": ["名称", "ルビ", "購", "隠"]}]}
"""
