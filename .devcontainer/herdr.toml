[keys]
prefix = "ctrl+k"

# Spaces の git_name（長い branch）が切れにくい幅
[ui]
sidebar_width = 40
sidebar_min_width = 28
sidebar_max_width = 56

[ui.sidebar.spaces]
rows = [
  ["state_icon", "workspace"],
  [
    { token = "$git_clean",  fg = "#50FA7B" },
    { token = "$git_staged", fg = "#E5A700" },
    { token = "$git_dirty",  fg = "#FF5555" },
  ],
  [{ token = "$git_name" }],
  [{ token = "$clock", fg = "#8BE9FD" }],
]
